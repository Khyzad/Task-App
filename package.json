{
  "name": "storyapp",
  "version": "1.0.0",
  "description": "A simple POC app that allows users to view and purchase stories'",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm start --prefix ./client",
    "server": "npm start --prefix ./server",
    "dev": "concurrently --kill-others \"npm run client\" \"npm run server\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.10"
  }
}
